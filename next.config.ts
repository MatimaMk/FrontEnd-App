import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ['aws.wideinfo.org', 'diyglow.in', 'www.dyln.co','static.wixstatic.com', 'www.bing.com', 'cdn.images.express.co.uk'], // Add all external image domains here
  },
};



export default nextConfig;
